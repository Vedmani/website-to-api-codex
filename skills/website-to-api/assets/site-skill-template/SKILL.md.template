---
name: SITE_NAME
description: "Use SITE_DISPLAY_NAME's internal API to retrieve or export SITE_CAPABILITY. Use when asked to SITE_TRIGGER_PHRASES for SITE_DISPLAY_NAME after its endpoints have been discovered with website-to-api."
---

# SITE_DISPLAY_NAME

Use `scripts/client.py` to call SITE_DISPLAY_NAME's internal API.

This skill was generated from `website-to-api`. It should contain only the files needed by Codex: `SKILL.md`, `agents/openai.yaml`, and required resources such as `scripts/`.

- Discovery verified: `YYYY-MM-DD`
- Internal API stability: undocumented and subject to change

## API

Base host: `SITE_API_BASE`

| Endpoint | Method | Purpose |
|---|---|---|
| `SITE_ENDPOINT` | `GET_OR_POST` | `SITE_ENDPOINT_PURPOSE` |

Document request bodies, query parameters, pagination, polling, or streaming details here.

## Auth

- Auth needed: `YES_OR_NO`
- Cookie/header names: `COOKIE_OR_HEADER_NAMES`
- Env vars: `SITE_UPPER_AUTH_COOKIE`, `SITE_UPPER_COOKIE_HEADER`, `SITE_UPPER_USER_AGENT`, or `NONE`
- Unauthenticated behavior: `DOCUMENT_STATUS_AND_RESPONSE`
- Authenticated behavior: `DOCUMENT_STATUS_AND_RESPONSE`

If auth is required, pass secrets through environment variables only. Never store cookie values, tokens, browser state, or raw auth headers in this skill.

Do not expose credential values as CLI arguments. Add a configurable User-Agent only when replay testing proves one is required.

## Commands

Run from this skill directory:

```bash
uv run scripts/client.py --help
```

Example:

```bash
uv run scripts/client.py SITE_COMMAND SITE_ARGUMENTS
```

If running from another directory, prefix the script path with the generated or installed skill path.

## Response Notes

Document the response fields used by the client here, including any event/chunk/reference structure for streamed or polled APIs.

## Safety

Document whether commands are read-only. Require explicit user approval before adding or invoking commands that purchase, message, upload, modify, or delete external data.

## If This Breaks

This skill uses SITE_DISPLAY_NAME's internal, undocumented API. If commands fail:

1. Read the error - 401/403 likely means expired cookie, renamed cookie, or changed auth checks.
2. Re-discover using the `website-to-api` meta-skill.
3. Update this skill's SKILL.md and scripts with the new API shape.
